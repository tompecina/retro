#! /bin/bash

source bin/common.makegraphics

for s in $seq; do
    w=$(($s*96))
    h=$(($s*16))
    convert $opts $filter $src/marking-3.png -alpha Set -channel A -evaluate Multiply $trans -resize ${w}x$h $dst/basic-3-$s.png
done
